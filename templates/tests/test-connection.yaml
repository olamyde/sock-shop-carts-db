apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sock-shop-carts.fullname" . }}-test-connection"
  labels:
    {{- include "sock-shop-carts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sock-shop-carts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
